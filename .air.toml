# .air.toml

# Directory watch
root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -buildvcs=false -o app ./cmd/server"
  bin = "app"
  include_ext = ["go", "tpl", "tmpl", "html"]

[log]
  time = true

[color]
  main = "cyan"
  watcher = "magenta"
  build = "yellow"
  runner = "green"